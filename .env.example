# harcompañon provider credentials — copy to `.env` (git-ignored) and fill in.
# Never commit real keys. Calls are stateless and direct to each provider's API.

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# OpenAI
OPENAI_API_KEY=

# Google (Gemini)
GEMINI_API_KEY=
